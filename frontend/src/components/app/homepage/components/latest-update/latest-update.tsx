import * as React from 'react';
import { CurrentlyReadingSection } from './styled';

const LatestUpdate: React.FunctionComponent = () => {
  return (
    <>
      <CurrentlyReadingSection>Currently reading</CurrentlyReadingSection>
    </>
  );
};

export default LatestUpdate;
